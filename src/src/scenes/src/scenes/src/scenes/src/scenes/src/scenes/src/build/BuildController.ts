/**
 * BuildController coordinates the UI and logic for build mode. It exposes
 * methods to begin placement of furniture, cancel placement and finalise
 * placements. This stub exists to scaffold later development.
 */
export default class BuildController {
  startPlacement(_itemId: string): void {
    // TODO: implement build placement logic
  }
  cancelPlacement(): void {
    // TODO: implement cancellation
  }
  confirmPlacement(): void {
    // TODO: confirm placement
  }
}
