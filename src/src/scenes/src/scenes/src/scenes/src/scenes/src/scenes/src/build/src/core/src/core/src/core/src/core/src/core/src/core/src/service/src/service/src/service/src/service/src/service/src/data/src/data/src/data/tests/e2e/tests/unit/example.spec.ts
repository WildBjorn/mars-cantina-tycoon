import { describe, it, expect } from 'vitest';

// A simple example test to verify that the testing infrastructure is
// functioning correctly. Additional unit tests should live in this folder.
describe('math', () => {
  it('adds numbers correctly', () => {
    expect(1 + 1).toBe(2);
  });
});
