/**
 * CustomerAI governs the behaviour of customers during service. It loads
 * preferences from `src/data/customers.json` and decides when to enter,
 * order and leave. This skeleton provides a simple interface for future
 * development.
 */
export default class CustomerAI {
  update(_dt: number): void {
    // TODO: implement customer behaviour
  }
}
