/**
 * The ServiceLoop orchestrates the serving of customers during the service
 * mode. It would handle spawning customers, taking orders, running
 * minigames and paying out earnings. This file provides a skeleton for
 * future development and currently contains no functionality.
 */
export default class ServiceLoop {
  start(): void {
    // TODO: implement service loop
  }
}
